# Evolucao da pesquisa de fichas tecnicas

Registro agregado, sem prompts, URLs, respostas cruas ou credenciais. Compare somente amostras equivalentes; uma cobertura maior nao e melhora se a evidencia for removida por aderencia ou autoridade.

| Data | Revisao/runtime | Amostra | Resultado | Evidencia e pipeline | Hipotese avaliada | Decisao |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-09-11 | worktree com P1-027 de priorização por variável no refine; `ex_prompt_compat`; OpenRouter/Gemini 2.5 Flash | Nova execução real Ford Ranger Raptor 2025 Brasil, mesma família do baseline pós-validação; orçamento e provider aparentam equivalentes, mas uma única repetição não prova causalidade | Ficha validada: 107/194 campos pesquisáveis preenchidos, 5 informados na entrada, 81 não encontrados, 6 não aplicáveis e 0 conflitos. São 2 campos resolvidos a menos que o baseline de 109. | Ficha validada: 2 fontes finais (1 `exata`, 1 `ambigua`), ambas referenciadas, sem referência inválida e sem múltiplas classes por campo. A classe `official` sustentou 14 campos resolvidos e `other`, 93; `partner`, 0. Provider próximo no tempo: quick com 3 observações e refine com 5, ambos JSON válido, sem remoções e sem erro. A correlação direta por `request_id` entre os dois eventos não ocorreu nesta amostra; a associação é temporal e serve apenas para diagnóstico de passes. | A instrução genérica de priorizar primeira parte por variável não produziu aumento mensurável de vínculos oficiais nesta repetição: a distribuição permaneceu 14 oficiais e a cobertura caiu de 109 para 107. Lucas aprovou rollback pontual: mantém-se `ex_prompt_compat` e a telemetria; reverte-se somente esta orientação experimental. | reverter |
| 2026-09-11 | worktree pós-correção de resumo validado; `ex_prompt_compat`; OpenRouter/Gemini 2.5 Flash | Execução real Ford Ranger Raptor 2025 Brasil; primeiro baseline com resultado pós-validação por classe, portanto não comparável numericamente aos eventos pré-validação anteriores | Ficha validada: 109/194 campos pesquisáveis preenchidos, 5 informados na entrada, 79 não encontrados, 6 não aplicáveis e 0 conflitos. | Provider: quick com 3 observações e refine com 5; ambos JSON válido, sem remoção por autoridade/aderência no estágio do provider. Ficha validada: 2 fontes finais (1 `exata`, 1 `ambigua`), ambas referenciadas e sem referência inválida. A fonte oficial aprovada sustentou 14 campos resolvidos; a outra fonte sustentou 95. Nenhum campo teve múltiplas classes/fontes; 109 vínculos foram de fonte única. | A fonte oficial é descoberta, publicada e usada, mas não é distribuída pela maior parte das variáveis. O próximo gargalo é alocação/corroboramento de evidência no refine, não descoberta oficial. | pronto_para_experimento |
| 2026-09-11 | worktree pós-P1-027 classe de fonte; `ex_prompt_compat`; OpenRouter/Gemini 2.5 Flash | Nova execução real Ford Ranger Raptor 2025 Brasil; comparação indicativa porque a telemetria de completude ainda é pré-validação | Resumo do provider: 125/203 preenchidas, 5 informadas na entrada, 67 não encontradas, 6 não aplicáveis e 0 conflitos. O operador observou aumento expressivo de fontes na ficha. | Quick: 3 fontes observadas; refine: 7; sem remoção por autoridade/aderência no evento. O evento pré-validação reportou 4 fontes (2 exatas, 1 ambígua, 1 divergente), 3 referenciadas e 125 vínculos; todos foram classificados como `other`. A causa foi confirmada no fluxo: `llm_execution` é gravado dentro de `callLLM`, antes de `validateResponse` aplicar a avaliação de política server-owned que classifica a resposta persistida. | A nova telemetria por classe não pode decidir ainda se fonte oficial respondeu variável, pois está no estágio anterior à validação/publicação. A melhoria observada de cobertura/fontes é promissora, mas não é prova de proveniência final por classe. | investigar |
| 2026-09-11 | `4ac8d8c`; `ex_prompt_compat`; OpenRouter/Gemini 2.5 Flash; telemetria P1-027 ativa | Nova execucao real Ford Ranger Raptor 2025 Brasil; comparavel apenas como repeticao indicativa, pois nao ha controle registrado de amostragem do provider | Interface: 110 variaveis. O `resumo_completude` nao entrou no evento porque nao satisfez o contrato estrito de inteiros; isto limita a comparacao estrutural, mas nao invalida a ficha exibida. | Quick: 4 fontes observadas; refine: 5; ambos finalizaram JSON valido e nao removeram fontes por autoridade ou aderencia. Duas fontes finais: 1 `exata` e 1 `ambigua`. Ambas foram referenciadas por ao menos um campo; 117 campos tiveram referencia valida, todos com uma unica fonte por campo e nenhum com multiplas fontes. Se a fonte oficial observada pelo operador integra as duas finais, ela foi usada ao menos uma vez, mas a telemetria sanitizada nao retem o mapeamento fonte-campo. | A descoberta de fonte oficial nao e, por si, o gargalo comprovado: a fonte pode ser publicada e pouco utilizada. A concentracao por campo persiste, e a P1-028 (pista de descoberta) nao resolve sozinha a decisao de usar/corroborar essa evidencia. | investigar |
| 2026-09-11 | `4ac8d8c`; `ex_prompt_compat`; OpenRouter/Gemini 2.5 Flash; telemetria P1-027 ativa | Nova execucao real Ford Ranger Raptor 2025 Brasil; mesma familia de alvo, mas sem pareamento de prompt/mercado/orcamento registrado para inferencia causal | Interface: 101/194 variaveis. Resumo estrutural: 102/199 preenchidas, 5 informadas na entrada, 86 nao encontradas, 6 nao aplicaveis e 0 conflitos. | Quick: 3 fontes observadas; refine: 9; ambos finalizaram JSON valido e nao removeram fontes por autoridade ou aderencia. Duas fontes finais, ambas `exata`. As duas foram referenciadas e nao houve referencia invalida. Todos os 102 campos vinculados tiveram uma unica fonte por campo; somente os grupos seguranca (24) e tecnologia dinamica (7) referenciaram as duas fontes no agregado. A telemetria intencionalmente nao classifica host como oficial; o operador nao observou fonte oficial publicada nesta ficha. | A queda de cobertura nao decorre de bloqueio server-side nem de falha de passe. A selecao final continua concentrada por campo, e a ausencia de fonte oficial observada pelo operador pode ser uma variacao da descoberta/decisao do modelo, ainda sem serie comparavel para acionar P1-028. | investigar |
| 2026-09-11 | `be8b1f6` + worktree; `ex_prompt_compat` com refine de diversidade da P0-010; OpenRouter/Gemini 2.5 Flash | Execucao real Ford Ranger Raptor 2025 Brasil, equivalente ao alvo da amostra anterior; mesmo provider/modelo e limite de turnos | 114/199 campos preenchidos no resumo estrutural; 107 no recorte de pesquisa exibido pela interface, pois 7 campos de identificação integram o total estrutural; 79 nao encontrados; 6 nao aplicaveis; 0 conflitos | Quick: 3 fontes observadas; refine: 9. Duas fontes finais: 1 `exata` e 1 `ambigua`; nenhum isolamento por autoridade/aderência; 114 campos confirmados com `fonte_ref` válido. O operador observou uma fonte Ford oficial entre as finais. | Remover a orientação estrita de parceiros do refine compatível aumenta a diversidade publicada sem reduzir cobertura: a ficha passou de uma para duas fontes finais e preservou vínculos por campo. A concentração em uma fonte ainda pode ocorrer por densidade real de evidência, não é automaticamente falha. | manter |
| 2026-09-11 | `be8b1f6` + worktree; modo `ex_prompt_compat`; OpenRouter/Gemini 2.5 Flash; telemetria `result_summary` ativa | Execucao real Ford Ranger Raptor 2025 Brasil; comparacao direta com a run anterior fica limitada pela ausencia de baseline identico | 111/199 variaveis preenchidas (55,8%); 82 nao encontradas; 6 nao aplicaveis; 0 conflitantes; ficha concluida com sucesso | Descoberta institucional, documental, bootstrap e leitor pulados. Quick: 3 fontes observadas; refine: 8. Uma fonte final com aderencia `exata`; nenhuma fonte removida por autoridade/aderencia. O novo resumo foi gravado; sua primeira contagem de `fonte_ref` foi invalidada por um bug local de telemetria que tratava a lista de IDs do schema como texto simples, corrigido sem alterar a pesquisa. | A correção de logging não alterou a busca. O contador de vínculos por campo desta execução deve ser reavaliado somente numa nova geração; não se infere ausência de referências a partir do registro anterior. | investigar |
| 2026-09-11 | `be8b1f6` + worktree; modo `ex_prompt_compat`; OpenRouter/Gemini 2.5 Flash; limite de 3 turnos | Execucao real mais recente; mesmo alvo Ford 2025 das tentativas recentes, mas nao ha pareamento seguro com uma execucao anterior de configuracao identica | Interface reportou 84 variaveis pesquisadas/preenchidas; requisicao concluida com sucesso. O snapshot agregado correspondente nao estava disponivel no diretorio de logs no momento da auditoria, portanto nao se infere distribuicao por status alem do contador apresentado na interface. | Descoberta institucional, documental, bootstrap e leitor: pulados pelo modo de compatibilidade. Passes: rapido com 3 fontes observadas e refinamento com 10; o usuario confirmou duas fontes finais oficiais. Sem erro de provider e sem remocao de evidencia por politica registrada nesta execucao. | O modo de compatibilidade do ex_prompt, ao preservar a busca ampla em dois passes e nao reter evidencia antes do refinamento, pode recuperar cobertura e ainda permitir que fontes oficiais sobrevivam quando o modelo as encontra. | manter |
| 2026-09-11 | `42c1d46` + worktree; `source-policy` 2026-09-08.2; `research-document-policy` 1.4.0 | Execucao real mais recente; marca sem dominio oficial configurado; nao comparavel a Ford configurada | 10/195 variaveis preenchidas (5,1%); ficha persistida | Descoberta de marca: 5 observacoes/2 candidatos. Descoberta documental oficial: pulada (0 dominios). Documento candidato: 0 elegiveis. Leitor e fetch: pulados. Passes: rapido 3 fontes, refinamento 6; aderencia isolou 1 e 2 fontes, respectivamente. | Sem uma ancora de primeira parte para a marca, a barreira de autoridade e a aderencia removem quase toda a evidencia antes do preenchimento. | investigar |
| 2026-09-11 | worktree com bootstrap institucional; mesmas politicas/modelo observados | Nova execucao manual do mesmo fluxo; total de variaveis divergiu (194 versus 195), portanto comparacao de cobertura e apenas indicativa | 26/194 variaveis preenchidas (13,4%); apenas uma fonte final, classificada pelo usuario como imprensa automotiva | Descoberta de marca: 5 observacoes/3 candidatos. Descoberta documental oficial, leitor e fetch do veiculo: pulados. Passes: rapido 3 fontes com 2 isoladas por aderencia; refinamento 7 fontes com 2 isoladas. Nenhuma fonte foi removida pela barreira de autoridade. Telemetria sanitizada nao expõe o resultado do bootstrap institucional. | O bootstrap nao promoveu host de primeira parte; sem esse host, a unica fonte aderente sobrevivente pode ser parceira de imprensa, mas nao resolve a cobertura oficial. | investigar |

## Como registrar a proxima execucao

1. Mantenha alvo, mercado, ano, provider/modelo e orcamento iguais ao baseline quando o objetivo for medir uma unica mudanca.
2. Acrescente uma linha com os campos do [contrato da auditoria](../../.codex/skills/technical-sheet-search-auditor/references/evolution-ledger.md).
3. Só compare cobertura e qualidade quando a coluna **Amostra** estiver equivalente; caso contrário, use o resultado apenas como diagnóstico.
4. Use `llm_execution` apenas para passes/orçamento do provider e `validated_technical_sheet_result` para cobertura, fontes publicadas/referenciadas e vínculo por campo. Quando `by_source_class` estiver disponível, registre campos resolvidos por classe `official`, `partner` e `other`; ausência da métrica em uma execução antiga deve ser anotada como indisponível, nunca estimada a partir do conteúdo exibido.
